import React from 'react';
import {WhatsUp} from "../../components";

const Header = () => {
    return (
        <div className="Header">
            <WhatsUp/>
        </div>
    );
};

export default Header;
