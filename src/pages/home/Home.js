import React from 'react';
import {Header, Body} from '../../containers'
const Home = () => {
    return (
        <div className="Home">
            <Header/>
            <Body/>
        </div>
    );
};

export default Home;
