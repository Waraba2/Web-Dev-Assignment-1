import React from 'react';
import {Header, Body} from '../../containers'
import {Router} from "react-router-dom";
const Home = () => {
    return (
        <div className="Home">
            <Header/>
            <Body/>
        </div>
    );
};

export default Home;
